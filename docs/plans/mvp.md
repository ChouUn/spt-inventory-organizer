# MVP：一键整理仓库

> Parent: [../index.md](../index.md)
> Status: 进行中
> 依据：[requirements.md](../requirements.md)、[arch.md](../arch.md)、
> [feats/tag-grammar.md](../feats/tag-grammar.md)、`reports/`

## 目标

点击容器面板的排序按钮，由本 mod 统一完成折叠、合并堆叠、按 tag 收纳与排布。
完成后玩家能观察到：可折叠武器已折叠，物品按规则进了容器，容器内布局紧凑，
Pinned 与 Locked 物品原地不动。

## 范围

### 包含

工程骨架、tag 解析与测试、tag 编辑提示与长度放宽、替换排序按钮、
快照与游戏适配、折叠、收纳、启发式排布、UIFixes 入口兼容、合并堆叠、CP-SAT 排布。

### 不包含

服务端组件、FiR 条件、预览与撤销、战局内可用、角色装备整理、发布打包与版本检查。

## 实现方案

- 工程面向 net481：OR-Tools 没有 netstandard2.0 目标，Core 只能选 net462 以上。
  测试工程用 xunit，在 Windows 的 dotnet 上跑。
- Plugin.Tests 链接实际兼容源码并使用本机 Harmony，独立验证补丁共存与精确移除，
  不加载 Unity 或游戏程序集；完整 Plugin 仍通过解决方案构建检查。
- Plugin 用 RID win-x64，OR-Tools 原生库随构建进入输出目录，整目录部署到
  `BepInEx/plugins/ChouUn.InventoryOrganizer/`。游戏程序集经 `SPT_DIR` 引用，不入库。
- Bootstrap 在 Awake 最早处 `SetDllDirectory(插件目录)`，依据是 spike 的结论。
- 排序按钮的替换点是 `GridSortPanel` 的点击处理，依据是折叠与锁的调研报告。
- UIFixes 兼容在 Plugin 侧实现：等待其补丁注册完成，核对目标与具体补丁方法后，
  精确移除 `GridSortPanel.Sort` 上的 `StackFirstPatch.Prefix`。
  不修改其配置、不移除其他补丁，也不调用其私有 `StackAll` 或 `Sort`。
- 合并堆叠使用游戏的匹配、合并与事务 API，由 Core 编排、GameAdapter 执行。
  合并范围、源与目标的锁处理、执行时机在该步骤实现前确定。
- 收纳阶段先用面积可行性判断，排布阶段先上启发式；CP-SAT 作为最后一步接入大件。

## 实施步骤

- [x] 1. 工程骨架
  - 结果：`src/Core`、`src/Plugin`、`tests/Core.Tests`、解决方案、部署脚本；
    Plugin 的 Awake 只做原生路径准备与日志。
  - 验证：构建与测试通过；游戏日志出现插件加载行（用户验收）。
- [x] 2. TagGrammar
  - 结果：按 feat 实现解析器，测试覆盖 feat 中每条规则、每种错误和全部示例。
  - 验证：测试通过。
- [x] 3. tag 编辑补丁
  - 结果：输入框上限放宽；保存时解析，成功复述规则，失败提示错误。
  - 验证：游戏内编辑 tag，看到提示（用户验收）。
- [x] 4. 排序按钮入口与快照
  - 结果：排序按钮的确认后入口被接管；GameAdapter 读出快照并写进日志；
    本步原生排序照常执行，替换留到步骤 5。
  - 验证：点击按钮，日志中的快照与仓库实际内容一致（用户验收）。
- [x] 5. 折叠阶段
  - 结果：折叠计划、simulate、提交；Locked 物品不动。
    原生排序暂作为整理的收尾保留，到步骤 7 由排布替换。
  - 验证：点击后可折叠武器折叠，被锁武器不变（用户验收）。
- [x] 6. 收纳阶段
  - 结果：Collector 按优先级调度，尊重容器接受性与面积可行性；
    Pinned 与 Locked 不动，无匹配留原地。
  - 验证：Core 测试覆盖调度顺序与跳过规则；游戏内物品按规则进容器（用户验收）。
- [x] 7. 启发式排布与 UIFixes 入口兼容
  - 结果：HeuristicPacker 与装箱接口；固定占位不动；
    排布范围为根容器加带有效 tag 的容器。
    布局经 ApplySortItemsPositionResult 走原生事务通道应用；
    按 F7 处理 UIFixes 入口冲突，避免其再启动排序。
  - 当前：用户游戏内验收通过（2026-09-10），确认收纳、折叠正确，速度明显改善。
    构建零警告、零错误；
    Core 70 项与 Plugin 4 项测试通过，包括冲突复现后修复和连续排布零变更。
  - 验证：测试覆盖不重叠、不越界、固定占位，以及兼容补丁的精确移除。
    分别检查安装与未安装 UIFixes；共存时开启其合并设置，确认一次点击仅整理一次，
    不进入 UIFixes Sort 或原生排序计算，布局保持且无变更时再次点击不提交排布。
    重新测量计算、事务与事件刷新耗时，由用户确认实际卡顿和布局表现。
  - 实测：UIFixes 6.0.2 入口移除成功；三次整理均无警告或失败，
    未记录到整理期间调用原生排序。首次重排 360 件为 1741 ms，
    其中事件刷新 1553 ms；后续收纳 3 件、无需排布为 32 ms，
    折叠 1 件并重排 1 件为 88 ms。快照单次 3–7 ms，布局计算 4–9 ms。
    未安装 UIFixes 的分支通过自动化测试，未另行卸载插件做游戏内验证。
- [ ] 8. 合并堆叠（步骤 7 验收通过后）
  - 结果：按 F8 确定合并语义并实现游戏 API 适配，合并后刷新受影响的快照。
  - 验证：测试覆盖数量守恒、堆叠上限、锁与模拟失败；
    游戏内验证合并生效，UIFixes 安装与否及其合并开关不影响本 mod 的合并功能。
    检查没有重复合并或额外排序，记录合并阶段耗时（用户验收）。
- [ ] 9. CP-SAT 排布
  - 结果：CpSatPacker 处理大件，1×1 交给启发式，带时间上限。
  - 验证：测试与启发式对比利用率不降；游戏内耗时在上限内（用户验收）。

## 验收

- Core 测试全部通过。
- Plugin 兼容测试全部通过，且完整解决方案构建通过。
- 用户在游戏内完成一次编辑 tag 与一次整理仓库的验收。
- 步骤 7 的入口兼容与性能复测通过后，再进入合并堆叠；合并验收后再接入 CP-SAT。

## 风险与阻塞

- 提交粒度已在步骤 5 定为逐项事务，与游戏自身操作一致；未通过模拟的单项放弃并记入结果。
  已回写 arch。
- 原生排序确认框已保留。
- UIFixes 6.0.2 入口冲突已修复并验收；首次大批量重排仍有事件刷新开销，
  不应把后续零变更的毫秒级耗时解读为大批量排布也已消除卡顿。
- 其他 UIFixes 版本的补丁签名和注册时机尚未验证；识别失败时必须明确报告兼容失败，
  不能按已消除冲突继续验收。

## 长期文档

- `feats/tag-grammar.md` 随实现同步；实现偏离时先确认再改契约。
- 提交粒度定下后回写 `arch.md` 关键约束。
- 需求 F 项如有偏离，先向用户确认。
- 入口兼容及合并分别验收后，更新根 README 的兼容状态与实际验证版本。
